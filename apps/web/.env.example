# API base URL for ClickHouse query proxy (Express `apps/server`).
# Start ClickHouse (`docker compose up -d`), then `bun run dev` in `apps/server`, then `apps/web`.
VITE_SERVER_URL=http://localhost:8080
